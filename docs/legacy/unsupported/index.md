# Unsupported

::: warning
The following feature is useless in most situations. Reading them is not recommended.
:::

Filters API is an easy-to-use library. It ensures that developers don't have to modify their codes when switching the Minecraft version. At the same time, **Filters API is an open library**. You can do almost anything you want, even if it is unsupported.

The following features are unsupported, they may require you to modify your codes when switching Minecraft. Even, causes unforeseen consequences.