# Disabling filters

Call `FilterBuilder.filtersVisibility(ItemGroup, boolean)` to disable or enable filters of the tab.

```java
FilterBuilder.filtersVisibility(CREATIVE_MODE_TAB, visible)
```

If visible is false, all highlighted components in the image will be invisible.

![](/disabling.png)